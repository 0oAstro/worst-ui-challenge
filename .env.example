# Supabase public configuration (RLS-safe)
# Do NOT add or use service role keys in this app runtime.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Base URL for auth redirects (adjust if not localhost)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
